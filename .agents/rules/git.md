# Git Workflow Rules

- **NO AUTOMATIC PUSHING**: You are allowed to `git commit` your work to save milestones, but you MUST NOT run `git push` automatically. The user will manually review and push the commits themselves.
